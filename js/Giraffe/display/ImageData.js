/**
 * Created by JetBrains PhpStorm.
 * User: Administrator
 * Date: 11-11-11
 * Time: 下午7:04
 * Dream: to be a Giraffe ~
 */
(function() {
    var ImageData = g.lib.createClass(
            function(src) {
                //TODO constructor
            }).extend({
                //TODO: functions
            });
    g.namespace("Giraffe.display.ImageData", ImageData);
})();